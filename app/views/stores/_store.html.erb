<tr>
  <td>
    <table>
      <tr>
        <td><%= store.name %></td>
        <td><%=t(:store_name)%></td>
      </tr>
      <tr>
        <td><%= store.url %></td>
        <td><%=t(:site)%></td>
      </tr>
      <tr>
        <td>
          <%= link_to t(:delete), store, :method => :delete, :confirm => t(:are_you_sure), :title => store.name %>
        </td>
      </tr>
    </table>
  </td>
</tr>
